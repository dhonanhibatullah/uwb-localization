class ULSTag {

    constructor(id, pos_x, pos_y) {
        
        this.info = {
            id: id,
            pos_x: pos_x,
            pos_y: pos_y,
        }

        this.map_info = map_info
    }

}

module.exports = ULSTag